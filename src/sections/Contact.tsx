import { ArrowUp, Check, Copy, Mail, Instagram, Github, Linkedin } from 'lucide-react';
import { useEffect, useRef, useState } from 'react';
import { links } from '../data/portfolio';
import { Reveal,SectionLabel } from '../components/Reveal';
export function Contact(){const[copied,setCopied]=useState(false);const[failed,setFailed]=useState(false);const timer=useRef<number|undefined>(undefined);useEffect(()=>()=>window.clearTimeout(timer.current),[]);async function copy(){try{await navigator.clipboard.writeText('mosamanoor17@gmail.com');setCopied(true);setFailed(false);window.clearTimeout(timer.current);timer.current=window.setTimeout(()=>setCopied(false),2500);}catch{setFailed(true);}}return <section id="contact" className="contact section"><Reveal><SectionLabel number="07">NEXT, LET’S CONNECT</SectionLabel><a className="closing-heading" href={links.email}><h2>Let’s build something <span>useful.</span></h2></a><div className="contact-details"><div><span className="eyebrow">HAVE SOMETHING IN MIND?</span><div className="email-row"><a href={links.email}>mosamanoor17@gmail.com</a><button className="icon-button" onClick={copy} aria-label="Copy email address">{copied?<Check size={18}/>:<Copy size={18}/>}</button></div><span role="status" className="copy-status">{copied?'Email copied.':failed?'Please select and copy the email address.':''}</span></div><div className="social-links"><a href={links.email}><Mail size={18} aria-hidden="true"/>Email </a><a href={links.instagram} target="_blank" rel="noreferrer"><Instagram size={18} aria-hidden="true"/>Instagram </a><a href={links.github} target="_blank" rel="noreferrer"><Github size={18} aria-hidden="true"/>GitHub </a><a href={links.linkedin} target="_blank" rel="noreferrer"><Linkedin size={18} aria-hidden="true"/>LinkedIn </a><a href={links.medium} target="_blank" rel="noreferrer"><svg width="20" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><ellipse cx="7" cy="12" rx="7" ry="7"/><ellipse cx="18" cy="12" rx="3.5" ry="6.5"/><ellipse cx="23" cy="12" rx="1" ry="6"/></svg>Medium </a></div></div></Reveal><footer><span>© {new Date().getFullYear()} MUHAMMAD OSAMA NOOR</span><span>CRAFTED WITH INTENT. BUILT TO CONNECT.</span><a href="#home">BACK TO TOP <ArrowUp size={14}/></a></footer></section>;}



